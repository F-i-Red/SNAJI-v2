
version: '3.9'

services:
  api:
    build: ../backend
    ports:
      - "8000:8000"

  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: snaji
      POSTGRES_USER: snaji
      POSTGRES_PASSWORD: snaji
    ports:
      - "5432:5432"
